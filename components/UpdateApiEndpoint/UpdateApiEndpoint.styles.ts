// UpdateApiEndpoint.styles.ts
import { createStyles, rem } from '@mantine/core';

const useStyles = createStyles((theme) => ({
    container: {
        display: 'flex',
        alignItems: 'center'
    },
    textField: {
        flex: 1,
        marginRight: theme.spacing.md,
    }
}));

export { useStyles };

